{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "{{platform === 'darwin' ? 'open' : platform === 'win32' ? 'start' : 'xdg-open'}} http://localhost:11436/config.html"
      }
    }
  ]
}
